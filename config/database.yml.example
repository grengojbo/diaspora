mysql: &mysql
  adapter: mysql2
  host: "localhost"
  port: 3306
  username: "root"
  password: ""
#  socket: /tmp/mysql.sock
  charset: utf8
  collation: utf8_bin

postgres: &postgres
  adapter: postgresql
  host: localhost
  port: 5432
  username: postgres
  password:
  encoding: unicode


common: &common
  # Choose one of the following
  <<: *mysql
  #<<: *postgres

# http://pullmonkey.com/2008/4/21/ruby-on-rails-multiple-database-connections/
# Multiple database connections
<% %w(development test production).each do |env| %>
<%= env %>:
  <<: *common
  #adapter: mysql
  database: diaspora_<%= env %>

#oracle_<%= env %>:
#  <<: *dev_basics
#  adapter: oracle
#  database: diaspora_oracle_<%= env %>
<% end %>

#test:
#  <<: *common
#  database: "diaspora_test<%= ENV['TEST_ENV_NUMBER'] %>"
